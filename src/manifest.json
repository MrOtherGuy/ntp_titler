{

  "description": "Adds a title prefix - for the current window based on the type of the active page",
	"author": "MrOtherGuy",
  "manifest_version": 2,
  "name": "ntp_titler",
  "permissions": ["tabs","browserSettings"],
  "version": "0.2.1",
	"background": {
		"scripts": ["background-script.js"]
	},
	"applications": {
  "gecko": {
    "id": "ntp_titler@example.com"
  }
}
}
